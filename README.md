# hash
A simple CLI tool to hash and verify passwords. If a text file is empty: The script hashes and saves your password.  If a hash exists: The script compares your input and returns "correct data" or "incorrect data".
